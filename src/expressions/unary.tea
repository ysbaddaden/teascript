prototype Unary
  def constructor(operator, right)
    self.operator = operator
    self.right = right
  end

  def compile
    rs = [ self.operator, self.right.compile() ]
    return self.operator == 'typeof' ? rs.join(' ') : rs.join('')
  end
end

module.exports = Unary
