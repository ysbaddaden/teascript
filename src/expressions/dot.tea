prototype Dot
  def constructor(left, right)
    self.left  = left
    self.right = right
  end

  def compile
    return self.left.compile() + '.' + self.right.compile()
  end
end

module.exports = Dot
