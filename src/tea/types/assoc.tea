object Tea.Assoc
  def init(left, right)
    self.left  = left
    self.right = right
    return self
  end

  def compile
    left = self.left instanceof Tea.Identifier ? self.left.value : self.left.compile()
    return left + ": " + self.right.compile()
  end
end
