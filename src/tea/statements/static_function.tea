object Tea.StaticFunction
  def init(name, args, body)
    self.fn = (new Tea.Function).init(name, args, body)
    return self
  end

  def compile(options)
    return self.fn.compile(prefix: options.objectName)
  end
end
