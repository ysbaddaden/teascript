object Tea.UnaryExpression
  def init(operator, right)
    self.operator = operator
    self.right = right
    return self
  end

  def compile
    if self.operator != 'typeof'
      return self.operator + self.right.compile()
    else
      return self.operator + ' ' + self.right.compile()
    end
  end
end
