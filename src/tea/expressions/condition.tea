object Condition
  def init(condition, a, b)
    self.condition = condition
    self.a = a
    self.b = b
    return self
  end

  def compile
    return self.condition.compile() + ' ? ' + self.a.compile() + ' : ' + self.b.compile()
  end
end

module.exports = Condition
