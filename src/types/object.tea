def TObject(declarations)
  self.declarations = declarations || [];

  def compile
    declarations = self.declarations.map(->(decl) { return decl.compile() })
    return "{" + declarations.join(", ") + "}";
  end
end

module.exports = TObject
