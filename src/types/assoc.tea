Identifier = require('../identifier')

def TAssoc(left, right)
  self.left  = left
  self.right = right

  def compile
    left = self.left instanceof Identifier ? self.left.value : self.left.compile()
    return left + ": " + self.right.compile()
  end
end

module.exports = TAssoc
