def ElsifStatement(test, body)
  self.test = test
  self.body = body

  def compile
    return '} else if (' + self.test.compile() + ') {' + self.body.compile();
  end
end

module.exports = ElsifStatement
