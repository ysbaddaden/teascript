prototype ThrowStatement
  def constructor(expression)
    self.expression = expression
  end

  def compile
    return 'throw ' + self.expression.compile() + ';'
  end
end

module.exports = ThrowStatement
