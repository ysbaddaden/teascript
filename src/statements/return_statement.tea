def ReturnStatement(expression)
  self.expression = expression

  def compile
    return 'return;' if self.expression == nil
    return 'return ' + self.expression.compile() + ';'
  end
end

module.exports = ReturnStatement
