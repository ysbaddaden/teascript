prototype RequireStatement
  def constructor(expression)
    self.expression = expression
  end

  def compile
    return 'require(' + self.expression.compile() + ')';
  end
end

module.exports = RequireStatement
