prototype UntilStatement
  def constructor(test, body)
    self.test = test
    self.body = body
  end

  def compile
    return 'while (!(' + self.test.compile() + ')) {' + self.body.compile() + '}'
  end
end

module.exports = UntilStatement
