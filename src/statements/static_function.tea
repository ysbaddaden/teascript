TFunction = require('./function')

prototype StaticFunction
  def constructor(name, args, body)
    self.fn = new TFunction(name, args, body)
  end

  def compile(options)
    return self.fn.compile(prefix: options.proto)
  end
end

module.exports = StaticFunction
