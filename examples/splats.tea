def join(*args)
  return args.join(" ")
end

def concat(a, b, *ary)
  x = [a, b].concat(ary)
  console.log("ary: ", ary)
  console.log("  x: ", x)
  return x
end

join(*[1, 2, 3])

args = [3, 4]
concat(1, 2, *args)
concat(1, 2, *args, 3, 4)
concat(1, 2, *args, 5, 6, 7, *[8, 9, 10])

