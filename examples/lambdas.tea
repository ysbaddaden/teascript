getResizedWidth = ->(x, y, by = 0.5) {
  return [ x * by, y * by ]
}

#var getRedizedSizes;
#getResizedSizes = function (x, y, by) {
#  if (typeof by === "undefined") {
#    by = 0.5;
#  }
#  return [ x * by, y * by ];
#};
#
#[ 
#  "var getRedizedSizes;",
#  "getResizedSizes = function (x, y, by) {",
#  [
#    "if (typeof by === \"undefined\") {",
#    [
#      "by = 0.5;"
#    ],
#    "}",
#    "return [ x * by, y * by ];"
#  ]
#  "};"
#]

