def my_empty_func
end

def emptyFunction
end

my_empty_func()
emptyFunction()

def emptyFunctionWithParens() end

def funcWithArg arg
  arg
end

# default values
def incr a, by = 1
  c = a + by
  return c
end

def decr(a, by = 1)
  return (
    a - by
  )
end

a = 1
b = incr(a)
b = incr(b, 4)
d = decr(c = a, 2)

# nested declarations
def parent
  def child
    def nestedWithArgs()
    end
  end
end

