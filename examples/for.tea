x = j = 0

j -= i for i of [ 1, 2, 3 ]

ary = [ 1, 2, 3, 4 ]
for i of ary do
  j += i
end

elements = document.getElementsByTagName("div");
for div of elements
  for elm of div.querySelectorAll(".hidden") do
    elm.classList.toggle("hidden");
  end
end

